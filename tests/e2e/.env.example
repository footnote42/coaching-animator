# E2E Test Environment Configuration
# Copy to .env.local and update values for your environment

# Base URL for tests (local development)
BASE_URL=http://localhost:3000

# Optional: Staging environment
# BASE_URL=https://staging-coaching-animator.vercel.app

# Test user credentials (optional, for pre-existing user)
# If not set, tests will generate new users with timestamps
# TEST_USER_EMAIL=test@example.com
# TEST_USER_PASSWORD=password123

# Playwright configuration
# Set to 'true' to see browser while tests run
# PLAYWRIGHT_HEADED=false

# Debug mode (shows step-by-step execution)
# PLAYWRIGHT_DEBUG=false
