# Staging Environment Configuration
# Created: 2026-01-31
# Description: Environment variables for staging deployment (testing before production)

# Next.js
NEXT_PUBLIC_SITE_URL=https://staging.your-domain.com

# Supabase (Staging Project)
# Create a separate Supabase project for staging: https://app.supabase.com
NEXT_PUBLIC_SUPABASE_URL=https://your-staging-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-staging-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-staging-service-role-key-here

# Database (use staging Supabase project's connection string)
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.your-staging-project.supabase.co:5432/postgres

# Feature Flags (optional - enable experimental features in staging)
NEXT_PUBLIC_ENABLE_DEBUG=true
NEXT_PUBLIC_ENABLE_EXPERIMENTAL_FEATURES=true

# Rate Limiting (more lenient in staging for testing)
RATE_LIMIT_CREATE_ANIMATION=20
RATE_LIMIT_UPLOAD_FILE=30

# Quota Limits (lower in staging to test quota enforcement)
QUOTA_MAX_ANIMATIONS_FREE=5
QUOTA_MAX_ANIMATIONS_AUTHENTICATED=20

# Logging
LOG_LEVEL=debug

# Notes:
# - Always use a separate Supabase project for staging (never point to production)
# - Use staging domain for NEXT_PUBLIC_SITE_URL
# - Test migrations in staging before applying to production
# - Keep staging data minimal and non-sensitive
# - Can be deployed to Vercel preview environment or separate staging deployment
